# semgrep.yml
configs:
  - p/owasp-top-ten
  - p/secrets
  - p/ci
  - rules/ubo-html-security.yml   # ⬅ agrega este ruleset local

paths:
  include:
    - "**/*.html"
  exclude:
    - "node_modules/**"
    - "dist/**"
    - "build/**"
